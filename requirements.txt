# HunyuanWorld-Mirror ComfyUI Node Pack - Dependencies
# Python 3.10+ required
#
# NOTE: This uses minimum version requirements to avoid conflicts with
# existing ComfyUI installations. If you have other custom nodes installed,
# their version requirements should be compatible.

# Core PyTorch (minimum versions - uses whatever is already installed if newer)
torch>=2.0.0
torchvision>=0.15.0

# NumPy (version < 2.0 for compatibility with many packages)
numpy>=1.24.0,<2.0.0

# Image/Video Processing
opencv-python>=4.6.0
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Deep Learning Libraries
einops>=0.7.0
timm>=0.6.7
transformers>=4.35.0
diffusers>=0.24.0

# Model Loading and Caching
huggingface-hub>=0.19.0
safetensors>=0.4.0

# 3D Processing and Export (required for core functionality)
trimesh>=4.0.0
onnxruntime>=1.16.0
plyfile>=1.0.0
open3d>=0.18.0
pycolmap>=0.6.0

# Visualization
matplotlib>=3.7.0

# Utilities
tqdm>=4.66.0
omegaconf>=2.3.0
scipy>=1.11.0

# gsplat dependencies (installed separately)
ninja
jaxtyping
rich

# flash-attn (OPTIONAL, installed separately if at all): models/layers/attention.py
# (WorldMirror 2.0) falls back to PyTorch SDPA when flash-attn isn't installed --
# functionally correct, just slower. Skip this entirely for a normal local install;
# only relevant if you're chasing the same throughput flash-attn gives on a
# Graydient-style deployment target. See UnlimitedEditing/flash-attention and
# KI-007 for a custom-built wheel matching a specific py3.12/torch2.11/cu128
# environment -- same reasoning as the gsplat wheel above, install from the
# matching prebuilt wheel or the official repo, don't add a bare "flash-attn"
# line here (it source-compiles, needing the CUDA toolkit -- same class of
# multi-hour build problem gsplat has).

# Optional: OpenEXR for depth export (may require system libraries)
# OpenEXR-Python>=1.3.0  # Uncomment if needed
